#!/usr/bin/python3
# Imports the variable `a` from the module `variable_load_5`.
from variable_load_5 import a

# Checks if the script is being run directly.
if __name__ != "__main__":
    exit()

# Prints the value of the variable `a`.
print(f"{a}")

